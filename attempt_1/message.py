class ClientMessage:

    def __init__(self, subheader_1, subheader_2, content):

    def create_subheader_1(param_len, instruct_type):

    def create_subheader_2(param):
        json_data = json.dumps(param)
        param_size = json_data.encode()
        

    
